package backend;

enum TraceMode {
    CONSOLE;    // Traces go to console only (original behavior)
    GAME;       // Traces go to in-game viewer only
    BOTH;       // Traces go to both console and in-game viewer
}
